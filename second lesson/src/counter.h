#pragma once
#include <string>

    class Counter {
        public:
                //void count (int a, void f(std::string)); если хотим чтобы метод принимал функции
               static void count (int a, std::string str);
    };
